from graphical.graphical import *
import tkinter as tk

def test_graphical():
    #open the graphical interface for choose the loading or the creation of the key
    load = Graphical()
    load.generation_key_window()
    #load.load_rule()